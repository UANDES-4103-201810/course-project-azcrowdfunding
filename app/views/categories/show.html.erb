<p>
  <strong>Tag:</strong>
  <%= @category.tag %>
</p>

<p>
  <strong>Project:</strong>
  <%= @category.project.id %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>

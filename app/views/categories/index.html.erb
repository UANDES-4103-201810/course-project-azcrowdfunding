<div class="container">
  <p id="notice"><%= notice %></p>
  <h1>Categories</h1>
  <%= link_to "All", projects_path, class: "btn btn-default btn-lg btn-block" %>
  <% @categories.each do |category| %>
    <%= link_to category.tag, show_by_cat_path(category.tag), class: "btn btn-default btn-lg btn-block" %>
  <% end %>
  <br>
</div>


